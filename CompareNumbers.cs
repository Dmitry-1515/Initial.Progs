// Input two numbers and compare them

string data = Console.ReadLine();

string data1 = Console.ReadLine();

int a = int.Parse(data);

int b = int.Parse(data1);

if (a == b)

    {
        Console.WriteLine("a = b");
    }

if (a > b)

    {
        Console.WriteLine("a > b");
    }

if (a < b)

    {
        Console.WriteLine("a < b");
    }

